spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:orderdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
<<<<<<< HEAD
jwt:
  secret: ${JWT_SECRET:SafeZoneSecretKeyForJWTAuthenticationMustBeAtLeast256BitsLong}
=======
logging:
  level:
    root: INFO

>>>>>>> 0836b0d (ci: guard Sonar CI scan to avoid Automatic Analysis conflict (#11))

logging:
  level:
    root: INFO
    "[com.safezone]": INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
